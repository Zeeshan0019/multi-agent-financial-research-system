# Add these to your project's existing requirements.txt
# (pydantic and reportlab are likely already present via FastAPI / Report Agent)
pydantic>=2.0
reportlab>=4.0
langchain-groq>=0.1  # optional: only needed for LLM-generated narrative
